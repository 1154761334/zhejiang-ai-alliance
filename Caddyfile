:80 {
    # Proxy API requests to Directus (docker container)
    handle_path /backend/* {
        reverse_proxy directus:8055
    }

    # Proxy everything else to Frontend (host machine)
    handle {
        reverse_proxy host.docker.internal:3000
    }
}
